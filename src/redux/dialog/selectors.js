export const selectDialogInfo = (state) => state.dialog.info;
export const selectDialogIsOpen = (state) => state.dialog.isOpen;
export const selectDialogChangeData = (state) => state.dialog.changeData;
